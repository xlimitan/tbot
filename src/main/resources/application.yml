spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/categories
    username: postgres
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN}